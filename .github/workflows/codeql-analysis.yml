name: "Code scanning - action"

on:
  push:
    branches:
      - develop
      - master
      - release/*
  pull_request:
  schedule:
    - cron: '0 19 * * 1'
  workflow_dispatch:
  
permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  CodeQL-Build:

    runs-on: ubuntu-latest
    permissions:
      security-events: write

    steps:
    - name: Checkout first revision
      uses: actions/checkout@v3
      with:
        ref: 3780b41dd070436f3f55327b0a88f27a52e2dfa8

        # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL (run 1)
      uses: github/codeql-action/init@v2
      with:
        languages: c-cpp
        db-location: '/home/runner/work/_temp/codeql_databases1/'
        debug: true

    #- name: Set env vars again
    #  run: |
    #    echo "BBCODEQL_EXPERIMENTAL_CPP_TRAP_CACHE=true" >> $GITHUB_ENV
    #    echo "BBCODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_WRITE=true" >> $GITHUB_ENV
    #    echo "BBCODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_DIR=/home/runner/work/_temp/cpp_trap_cache" >> $GITHUB_ENV

    - name: Build (create trap cache)
      env:
        CODEQL_EXPERIMENTAL_CPP_TRAP_CACHE: true
        CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_WRITE: true
        CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_DIR: /home/runner/work/_temp/cpp_trap_cache
      run: |
        echo "Here are the trap cache variables:"
        echo $CODEQL_EXPERIMENTAL_CPP_TRAP_CACHE
        echo $CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_WRITE
        echo $CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_DIR
        mkdir tmp
        cd tmp
        cmake ..
        make

    # Autobuild attempts to build any compiled languages  (C/C++, C#, or Java).
    # If this step fails, then you should remove it and run the build manually (see below)
    # - name: Autobuild (create cache)
    #   uses: github/codeql-action/autobuild@v2
      # Set the database folder to the default location

    #- name: Delete the database
    #  run: rm -rf /home/runner/work/_temp/codeql_databases

    - name: Checkout second revision
      uses: actions/checkout@v3
      with:
        # ref: 0457de21cffb298c22b629e538036bfeb96130b7
        # This is the initial ref: 3780b41dd070436f3f55327b0a88f27a52e2dfa8
        ref: 3780b41dd070436f3f55327b0a88f27a52e2dfa8

    # - name: Perform CodeQL Analysis
    #  uses: github/codeql-action/analyze@v2
    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL for second run
      uses: github/codeql-action/init@v2
      with:
        languages: c-cpp
        debug: true
  
    # - name: Set env vars for read
    #  run: |
    #    echo "YYCODEQL_EXPERIMENTAL_CPP_TRAP_CACHE=true" >> $GITHUB_ENV
    #    echo "YYCODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_WRITE=false" >> $GITHUB_ENV
    #    echo "YYCODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_DIR=/home/runner/work/_temp/cpp_trap_cache" >> $GITHUB_ENV
  
    - name: Build (read cache)
      env:
        CODEQL_EXPERIMENTAL_CPP_TRAP_CACHE: true
        CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_WRITE: false
        CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_DIR: /home/runner/work/_temp/cpp_trap_cache
      run: |
        echo "Here are the trap cache variables:"
        echo $CODEQL_EXPERIMENTAL_CPP_TRAP_CACHE
        echo $CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_WRITE
        echo $CODEQL_EXTRACTOR_CPP_OPTION_TRAP_CACHE_DIR
        mkdir tmp
        cd tmp
        cmake ..
        make

    # - name: Autobuild (read cache)
    #  uses: github/codeql-action/autobuild@v2

    # TODO: Run the queries
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
